/*
缓冲区是为了提高流的操作效率而出现的。

所以在创建缓冲区之前，来先有流对象。

该缓冲区中提供了一个跨平台的换行符:newLine().

*/

import java.io.*;

class  BufferWriterDemo
{
	public static void main(String[] args) throws Exception 
	{
		FileWriter fw = new FileWriter("d.txt");         //创建一个字符写入流对象。

		BufferedWriter bufw = new BufferedWriter(fw);    //将需要被提高效率的流对象作为参数传递给缓冲区的构造函数即可。

		for(int x=0 ; x<5 ; x++)
		{
			bufw.write("abcd"+x);
			bufw.newLine();
			bufw.flush();                               //********************记住，只要用到缓冲区，既要记得刷新。**********************
		}

		bufw.close();                                   //其实，关闭缓冲区，就是关闭缓冲区中的流对象。
	}
}
